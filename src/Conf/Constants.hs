module Conf.Constants
  ( app
  , templateTable
  , templateTableHeaders
  ) where

{- | Constants for the application. -}

{- | The name of the whole application. -}
app = "hilda"

{- | The name of the table for templates. -}
templateTable = "templates"

{- | The headers for the template table. -}
templateTableHeaders = ["id", "path"]
